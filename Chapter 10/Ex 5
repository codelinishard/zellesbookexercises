class Student:
    
    def __init__(self, name, hours, qpoints):
        self.name = name
        self.hours = float(hours)
        self.qpoints = float(qpoints
    def getName(self):
        return self.name
    def getHours(self):
        return self.hours
    def getQPoints(self):
        return self.qpoints
    def gpa(self):
        return self.qpoints/self.hours


    def addGrade(self, gradePoint, credits):
        self.qpoints += gradePoint * credits
        self.hours += credits

def main():
    student = Student("name irrelevant", 0, 0)
    while True:
        gp_and_credit = input("""Input the gradePoint and credits of a course, 
separated by a space. Type 't' to terminate.""")
        if gp_and_credit == "t":
            break
        else:
            gp, credit = gp_and_credit.split()
            student.addGrade(float(gp),float(credit))
    
    try: print(student.gpa())
    except: print("There was no input given. GPA is not calculated")

if __name__ == '__main__':
    main()

